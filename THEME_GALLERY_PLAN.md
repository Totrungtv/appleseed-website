# Apple Seed Home Theme Gallery V1

30 named homepage themes are cataloged in `home-themes.json`.

Safety boundary: this feature is developed on branch `feature/homepage-theme-gallery-v1`, based on the verified production commit `90c4b39c2fb08c71933cd68f0f086ef955fcf84f`. It is not merged to `main` yet.

Next integration step: wire the catalog into Site Builder/Admin and wire the selected published theme into `index.html`, with preview before publish and no replacement of existing content data.
