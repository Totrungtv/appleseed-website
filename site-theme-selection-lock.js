/* APPLE SEED THEME SELECTION LOCK V4
   Intentionally inert. Theme selection is handled by the single bridge controller.
   This file remains only for backward-compatible HTML references; it performs no polling,
   no MutationObserver work, and no iframe/class rewriting.
*/
(function(){'use strict';})();
